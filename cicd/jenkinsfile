pipeline {
    agent any
    stages {
        stage ('Install dependencies') {
            steps {
                sh "apk add nodejs"
                sh "echo $PATH"
                sh "npm install"
            }
        }
        stage('Unit Test') {
            steps {
                 sh 'npm run test'
            }
        }
    }
}